{
  "version": "v1",
  "hooks": [
    {
      "name": "destructive-shell-guard",
      "description": "Lightweight agent-side guardrail appended before shell tool use",
      "trigger": "PreToolUse",
      "matcher": "shell|execute_bash",
      "action": {
        "type": "agent",
        "prompt": "Guardrail: if this shell command is destructive (cdk destroy, deploy.sh --delete, rm -rf, git push --force, emptying S3 buckets, deleting log groups or databases), verify the user explicitly requested that teardown in this session before running it. Migrations are deploys, never teardowns."
      }
    },
    {
      "name": "log-changes-round",
      "description": "On-demand: append the current change-set to CHANGES.md per steering/change-logging.md",
      "trigger": "Manual",
      "action": {
        "type": "agent",
        "prompt": "Append the current changeset as a new 'Round N' entry to the project's CHANGES.md, following steering/change-logging.md exactly (next round number, current local timestamp with offset, one bullet per discrete change)."
      }
    }
  ]
}
